---
/**
 * FIDUBIT - Remittance & Cross-Border Payments
 * International money transfer and payment solutions
 */
import Layout from '@/layouts/Layout.astro';
import Header from '@/components/layout/Header.astro';
import Footer from '@/components/layout/Footer.astro';
import WhatsAppButton from '@/components/ui/WhatsAppButton.astro';
import GlassPanel from '@/components/ui/GlassPanel.astro';
import Button from '@/components/ui/Button.astro';

const seo = {
  title: 'Remittance & Cross-Border Payments - FIDUBIT',
  description: 'International remittance solutions for Latin America, USA, Europe, and Asia. Collect payments, pay suppliers, and process payroll across borders with competitive rates and fast settlement.',
  keywords: [
    'remittance',
    'international money transfer',
    'cross-border payments',
    'send money to colombia',
    'send money to mexico',
    'send money to brazil',
    'payroll processing',
    'supplier payments',
    'collect payments internationally',
    'LATAM payments',
    'Europe payments',
    'Asia payments',
  ],
};

const regions = [
  {
    name: 'Latin America',
    countries: [
      { name: 'Colombia', flag: '🇨🇴', currency: 'COP', features: ['Bank Transfer', 'Cash Pickup', 'Mobile Wallet'] },
      { name: 'Mexico', flag: '🇲🇽', currency: 'MXN', features: ['Bank Transfer', 'SPEI', 'Cash Pickup'] },
      { name: 'Brazil', flag: '🇧🇷', currency: 'BRL', features: ['PIX', 'Bank Transfer', 'Boleto'] },
    ],
    color: 'yellow',
  },
  {
    name: 'North America',
    countries: [
      { name: 'United States', flag: '🇺🇸', currency: 'USD', features: ['ACH', 'Wire Transfer', 'Same-Day ACH'] },
      { name: 'Canada', flag: '🇨🇦', currency: 'CAD', features: ['EFT', 'Interac', 'Wire Transfer'] },
    ],
    color: 'blue',
  },
  {
    name: 'Europe',
    countries: [
      { name: 'United Kingdom', flag: '🇬🇧', currency: 'GBP', features: ['Faster Payments', 'BACS', 'CHAPS'] },
      { name: 'EU Zone', flag: '🇪🇺', currency: 'EUR', features: ['SEPA', 'SEPA Instant', 'SWIFT'] },
      { name: 'Switzerland', flag: '🇨🇭', currency: 'CHF', features: ['SIC', 'SWIFT'] },
    ],
    color: 'purple',
  },
  {
    name: 'Asia Pacific',
    countries: [
      { name: 'Hong Kong', flag: '🇭🇰', currency: 'HKD', features: ['FPS', 'RTGS', 'SWIFT'] },
      { name: 'Singapore', flag: '🇸🇬', currency: 'SGD', features: ['FAST', 'GIRO', 'PayNow'] },
      { name: 'China', flag: '🇨🇳', currency: 'CNY', features: ['CNAPS', 'Alipay', 'WeChat Pay'] },
    ],
    color: 'green',
  },
];

const services = [
  {
    icon: 'solar:wallet-money-linear',
    title: 'Collect Payments',
    description: 'Receive payments from customers worldwide. We provide local collection accounts in multiple countries so your customers can pay in their preferred method.',
    features: [
      'Local bank accounts in 15+ countries',
      'Multi-currency collection',
      'Real-time payment notifications',
      'Automated reconciliation',
    ],
  },
  {
    icon: 'solar:hand-money-linear',
    title: 'Pay Suppliers',
    description: 'Pay your international suppliers quickly and cost-effectively. Competitive FX rates with transparent fees and full payment tracking.',
    features: [
      'Pay in 50+ currencies',
      'Competitive exchange rates',
      'Batch payment processing',
      'Invoice management',
    ],
  },
  {
    icon: 'solar:users-group-rounded-linear',
    title: 'Global Payroll',
    description: 'Pay your remote team anywhere in the world. Compliant payroll processing with local tax handling and multiple payout options.',
    features: [
      'Multi-country payroll',
      'Contractor payments',
      'Tax compliance support',
      'Flexible payout methods',
    ],
  },
];

const stats = [
  { value: '50+', label: 'Currencies', icon: 'solar:dollar-linear' },
  { value: '180+', label: 'Countries', icon: 'solar:global-linear' },
  { value: 'T+0', label: 'Settlement', icon: 'solar:clock-circle-linear' },
  { value: '0.5%', label: 'From Fees', icon: 'solar:tag-price-linear' },
];

const colorClasses: Record<string, { bg: string; border: string; text: string }> = {
  yellow: { bg: 'bg-yellow-500/10', border: 'border-yellow-500/30', text: 'text-yellow-500' },
  blue: { bg: 'bg-blue-500/10', border: 'border-blue-500/30', text: 'text-blue-500' },
  purple: { bg: 'bg-purple-500/10', border: 'border-purple-500/30', text: 'text-purple-500' },
  green: { bg: 'bg-green-500/10', border: 'border-green-500/30', text: 'text-green-500' },
};
---

<Layout seo={seo}>
  <WhatsAppButton />
  <Header />

  <main class="relative z-10 pt-16 min-h-screen bg-fidubit-black">
    
    <!-- Hero Section -->
    <section class="relative py-24 border-b border-white/5 overflow-hidden">
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_top_center,rgba(234,179,8,0.08)_0%,transparent_50%)]"></div>
      <div class="absolute inset-0 grid-pattern opacity-20"></div>
      
      <div class="max-w-6xl mx-auto px-6 relative z-10">
        <div class="text-center max-w-3xl mx-auto">
          <!-- Breadcrumb -->
          <div class="flex items-center justify-center gap-2 text-xs font-mono text-zinc-600 mb-8">
            <a href="/" class="hover:text-white transition-colors">Home</a>
            <span>/</span>
            <span class="text-yellow-500">Remittance</span>
          </div>
          
          <!-- Badge -->
          <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-yellow-500/10 border border-yellow-500/20 mb-8">
            <span class="w-2 h-2 bg-yellow-500 rounded-full animate-pulse"></span>
            <span class="text-xs font-mono text-yellow-400 uppercase tracking-wider">Cross-Border Payments</span>
          </div>
          
          <!-- Title -->
          <h1 class="text-5xl md:text-6xl lg:text-7xl font-medium text-white uppercase tracking-tight mb-6 leading-[0.9]">
            Global
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 to-orange-500 block">
              Remittance
            </span>
          </h1>
          
          <!-- Description -->
          <p class="text-lg text-zinc-400 font-mono leading-relaxed mb-10 max-w-2xl mx-auto">
            Send and receive money across borders. Collect payments, pay suppliers, and process payroll 
            in Latin America, USA, Europe, and Asia with competitive rates.
          </p>
          
          <!-- CTA Buttons -->
          <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
            <Button 
              variant="primary" 
              href="#contact-remittance" 
              icon="solar:arrow-right-linear"
              size="lg"
            >
              Get Started
            </Button>
            <Button 
              variant="outline" 
              href="#coverage"
              icon="solar:global-linear"
              iconPosition="left"
            >
              View Coverage
            </Button>
          </div>
        </div>
      </div>
    </section>

    <!-- Stats -->
    <section class="border-b border-white/5 py-12">
      <div class="max-w-7xl mx-auto px-6">
        <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
          {stats.map((stat) => (
            <GlassPanel padding="md" rounded="lg" class="text-center hover:border-yellow-500/30 transition-colors">
              <iconify-icon icon={stat.icon} class="text-yellow-500 text-2xl mb-2"></iconify-icon>
              <div class="text-2xl font-bold text-white">{stat.value}</div>
              <div class="text-[10px] text-zinc-500 font-mono uppercase tracking-wide">{stat.label}</div>
            </GlassPanel>
          ))}
        </div>
      </div>
    </section>

    <!-- Services -->
    <section class="border-b border-white/5 py-20">
      <div class="max-w-7xl mx-auto px-6">
        <div class="text-center mb-16">
          <h2 class="text-3xl font-medium text-white uppercase tracking-tight mb-4">
            Payment Solutions
          </h2>
          <p class="text-zinc-500 font-mono text-sm max-w-xl mx-auto">
            Whether you need to collect, send, or pay - we've got you covered.
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          {services.map((service) => (
            <GlassPanel padding="lg" rounded="xl" class="hover:border-yellow-500/30 transition-all group">
              <div class="w-14 h-14 rounded-xl bg-yellow-500/10 border border-yellow-500/20 flex items-center justify-center mb-6 group-hover:bg-yellow-500/20 transition-colors">
                <iconify-icon icon={service.icon} class="text-yellow-500 text-2xl"></iconify-icon>
              </div>
              
              <h3 class="text-xl font-medium text-white mb-3">{service.title}</h3>
              <p class="text-sm text-zinc-500 font-mono leading-relaxed mb-6">{service.description}</p>
              
              <ul class="space-y-2">
                {service.features.map((feature) => (
                  <li class="flex items-center gap-2 text-xs font-mono text-zinc-400">
                    <iconify-icon icon="solar:check-circle-linear" class="text-yellow-500"></iconify-icon>
                    {feature}
                  </li>
                ))}
              </ul>
            </GlassPanel>
          ))}
        </div>
      </div>
    </section>

    <!-- Coverage Map -->
    <section id="coverage" class="border-b border-white/5 py-20">
      <div class="max-w-7xl mx-auto px-6">
        <div class="text-center mb-16">
          <h2 class="text-3xl font-medium text-white uppercase tracking-tight mb-4">
            Global Coverage
          </h2>
          <p class="text-zinc-500 font-mono text-sm max-w-xl mx-auto">
            Send and receive money in these regions with local payment rails.
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
          {regions.map((region) => {
            const colors = colorClasses[region.color];
            return (
              <GlassPanel padding="lg" rounded="xl" class="hover:border-white/20 transition-all">
                <!-- Region Header -->
                <div class="flex items-center gap-3 mb-6 pb-4 border-b border-white/5">
                  <div class:list={["w-10 h-10 rounded-lg flex items-center justify-center", colors.bg, colors.border, "border"]}>
                    <iconify-icon icon="solar:global-linear" class:list={[colors.text]}></iconify-icon>
                  </div>
                  <div>
                    <h3 class="text-lg font-medium text-white">{region.name}</h3>
                    <span class="text-[10px] font-mono text-zinc-600 uppercase">{region.countries.length} Countries</span>
                  </div>
                </div>
                
                <!-- Countries -->
                <div class="space-y-4">
                  {region.countries.map((country) => (
                    <div class="flex items-start justify-between p-4 rounded-lg bg-white/[0.02] border border-white/5 hover:border-white/10 transition-colors">
                      <div class="flex items-center gap-3">
                        <span class="text-2xl">{country.flag}</span>
                        <div>
                          <div class="text-white font-medium">{country.name}</div>
                          <div class="text-[10px] font-mono text-zinc-500">{country.currency}</div>
                        </div>
                      </div>
                      <div class="flex flex-wrap gap-1 max-w-[150px] justify-end">
                        {country.features.map((feature) => (
                          <span class="text-[8px] font-mono px-1.5 py-0.5 rounded bg-white/5 text-zinc-500">
                            {feature}
                          </span>
                        ))}
                      </div>
                    </div>
                  ))}
                </div>
              </GlassPanel>
            );
          })}
        </div>
      </div>
    </section>

    <!-- How It Works -->
    <section class="border-b border-white/5 py-20">
      <div class="max-w-5xl mx-auto px-6">
        <div class="text-center mb-16">
          <h2 class="text-3xl font-medium text-white uppercase tracking-tight mb-4">
            How It Works
          </h2>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
          <div class="text-center">
            <div class="w-16 h-16 rounded-full bg-yellow-500/10 border border-yellow-500/30 flex items-center justify-center mx-auto mb-4 text-yellow-500 text-xl font-bold">
              1
            </div>
            <h3 class="text-white font-medium mb-2">Create Account</h3>
            <p class="text-xs font-mono text-zinc-500">Quick onboarding with KYC verification</p>
          </div>
          
          <div class="text-center">
            <div class="w-16 h-16 rounded-full bg-yellow-500/10 border border-yellow-500/30 flex items-center justify-center mx-auto mb-4 text-yellow-500 text-xl font-bold">
              2
            </div>
            <h3 class="text-white font-medium mb-2">Add Beneficiary</h3>
            <p class="text-xs font-mono text-zinc-500">Enter recipient details and bank info</p>
          </div>
          
          <div class="text-center">
            <div class="w-16 h-16 rounded-full bg-yellow-500/10 border border-yellow-500/30 flex items-center justify-center mx-auto mb-4 text-yellow-500 text-xl font-bold">
              3
            </div>
            <h3 class="text-white font-medium mb-2">Fund Transfer</h3>
            <p class="text-xs font-mono text-zinc-500">Send funds via bank or crypto</p>
          </div>
          
          <div class="text-center">
            <div class="w-16 h-16 rounded-full bg-green-500/10 border border-green-500/30 flex items-center justify-center mx-auto mb-4 text-green-500">
              <iconify-icon icon="solar:check-circle-linear" width="28"></iconify-icon>
            </div>
            <h3 class="text-white font-medium mb-2">Delivered</h3>
            <p class="text-xs font-mono text-zinc-500">Recipient gets funds same day</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Use Cases -->
    <section class="border-b border-white/5 py-20 bg-fidubit-dark">
      <div class="max-w-7xl mx-auto px-6">
        <div class="text-center mb-16">
          <h2 class="text-3xl font-medium text-white uppercase tracking-tight mb-4">
            Common Use Cases
          </h2>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          <div class="p-6 rounded-xl border border-white/5 hover:border-yellow-500/30 transition-colors group">
            <iconify-icon icon="solar:programming-linear" class="text-3xl text-zinc-600 group-hover:text-yellow-500 mb-4 transition-colors"></iconify-icon>
            <h3 class="text-white font-medium mb-2">Remote Teams</h3>
            <p class="text-xs font-mono text-zinc-500">Pay your distributed engineering team in their local currency</p>
          </div>
          
          <div class="p-6 rounded-xl border border-white/5 hover:border-yellow-500/30 transition-colors group">
            <iconify-icon icon="solar:box-linear" class="text-3xl text-zinc-600 group-hover:text-yellow-500 mb-4 transition-colors"></iconify-icon>
            <h3 class="text-white font-medium mb-2">E-Commerce</h3>
            <p class="text-xs font-mono text-zinc-500">Collect payments from customers in LATAM and Europe</p>
          </div>
          
          <div class="p-6 rounded-xl border border-white/5 hover:border-yellow-500/30 transition-colors group">
            <iconify-icon icon="solar:buildings-2-linear" class="text-3xl text-zinc-600 group-hover:text-yellow-500 mb-4 transition-colors"></iconify-icon>
            <h3 class="text-white font-medium mb-2">Supplier Payments</h3>
            <p class="text-xs font-mono text-zinc-500">Pay manufacturers and vendors in Asia efficiently</p>
          </div>
          
          <div class="p-6 rounded-xl border border-white/5 hover:border-yellow-500/30 transition-colors group">
            <iconify-icon icon="solar:home-2-linear" class="text-3xl text-zinc-600 group-hover:text-yellow-500 mb-4 transition-colors"></iconify-icon>
            <h3 class="text-white font-medium mb-2">Family Support</h3>
            <p class="text-xs font-mono text-zinc-500">Send money to family members abroad quickly and safely</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Contact Section -->
    <section id="contact-remittance" class="py-24 relative">
      <div class="absolute inset-0 bg-[radial-gradient(circle_at_bottom_right,rgba(234,179,8,0.05)_0%,transparent_50%)]"></div>
      
      <div class="max-w-4xl mx-auto px-6 relative z-10">
        <div class="text-center mb-12">
          <h2 class="text-3xl font-medium text-white uppercase tracking-tight mb-4">
            Start Sending Money
          </h2>
          <p class="text-zinc-500 font-mono text-sm">
            Tell us about your payment needs and we'll set up your account.
          </p>
        </div>

        <GlassPanel padding="lg" rounded="2xl" class="relative overflow-hidden">
          <div class="absolute top-0 right-0 w-32 h-32 bg-yellow-500/10 blur-[60px] rounded-full pointer-events-none"></div>
          
          <form class="space-y-6">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div class="space-y-2">
                <label class="text-xs font-mono text-zinc-400 uppercase tracking-wide">Company Name</label>
                <input type="text" placeholder="Your Company" class="smart-input w-full" required />
              </div>
              <div class="space-y-2">
                <label class="text-xs font-mono text-zinc-400 uppercase tracking-wide">Email</label>
                <input type="email" placeholder="you@company.com" class="smart-input w-full" required />
              </div>
            </div>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div class="space-y-2">
                <label class="text-xs font-mono text-zinc-400 uppercase tracking-wide">Primary Use Case</label>
                <select class="smart-input w-full appearance-none cursor-pointer">
                  <option value="" class="bg-black">Select use case</option>
                  <option value="payroll" class="bg-black">Payroll / Contractor Payments</option>
                  <option value="suppliers" class="bg-black">Supplier / Vendor Payments</option>
                  <option value="collections" class="bg-black">Payment Collections</option>
                  <option value="personal" class="bg-black">Personal Remittance</option>
                </select>
              </div>
              <div class="space-y-2">
                <label class="text-xs font-mono text-zinc-400 uppercase tracking-wide">Monthly Volume</label>
                <select class="smart-input w-full appearance-none cursor-pointer">
                  <option value="" class="bg-black">Select volume</option>
                  <option value="0-10k" class="bg-black">$0 - $10,000</option>
                  <option value="10k-50k" class="bg-black">$10,000 - $50,000</option>
                  <option value="50k-250k" class="bg-black">$50,000 - $250,000</option>
                  <option value="250k+" class="bg-black">$250,000+</option>
                </select>
              </div>
            </div>

            <div class="space-y-2">
              <label class="text-xs font-mono text-zinc-400 uppercase tracking-wide">Countries You Need</label>
              <div class="grid grid-cols-2 md:grid-cols-4 gap-3">
                {['Colombia', 'Mexico', 'Brazil', 'USA', 'UK', 'Europe', 'Hong Kong', 'Singapore'].map((country) => (
                  <label class="flex items-center gap-2 p-3 rounded-lg bg-white/[0.02] border border-white/5 hover:border-yellow-500/30 cursor-pointer transition-colors">
                    <input type="checkbox" name="countries" value={country.toLowerCase()} class="accent-yellow-500" />
                    <span class="text-xs font-mono text-zinc-400">{country}</span>
                  </label>
                ))}
              </div>
            </div>

            <div class="space-y-2">
              <label class="text-xs font-mono text-zinc-400 uppercase tracking-wide">Additional Details</label>
              <textarea 
                rows="3"
                placeholder="Tell us more about your payment flows..."
                class="smart-input w-full resize-none"
              ></textarea>
            </div>

            <button type="submit" class="btn-cta w-full group">
              <span>Request Access</span>
              <iconify-icon icon="solar:arrow-right-linear" class="group-hover:translate-x-1 transition-transform" width="16"></iconify-icon>
            </button>
          </form>
        </GlassPanel>
      </div>
    </section>

    <Footer />
  </main>
</Layout>

<script>
  // Intersection Observer for scroll animations
  const observerOptions: IntersectionObserverInit = {
    root: null,
    rootMargin: '0px',
    threshold: 0.1
  };

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('sys-active');
        observer.unobserve(entry.target);
      }
    });
  }, observerOptions);

  document.querySelectorAll('.sys-reveal').forEach(el => {
    observer.observe(el);
  });
</script>
